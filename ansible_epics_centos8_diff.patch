diff --git a/centos8/Dockerfile b/centos8/Dockerfile
index ed3c558..4149502 100644
--- a/centos8/Dockerfile
+++ b/centos8/Dockerfile
@@ -1,6 +1,9 @@
 # The centos8 builder
 FROM centos:8
 
+ENV http_proxy=http://proxy:8888
+ENV https_proxy=http://proxy:8888
+
 # check for updates and ugrade packages
 RUN dnf check-update || { rc=$?; [ "$rc" -eq 100 ] && exit 0; exit "$rc"; }
 RUN dnf -y update
@@ -15,7 +18,7 @@ RUN dnf -y install git wget tar make cmake gcc gcc-c++ pkgconfig perl-devel
 
 # Packages required for building EPICS
 RUN dnf -y install libtirpc-devel
-RUN dnf -y --enablerepo=PowerTools install re2c libusb-devel rpcgen
+RUN dnf -y --enablerepo=powertools install re2c libusb-devel rpcgen
 RUN dnf -y install libxml2-devel pcre-devel libXext-devel
 RUN dnf -y install libjpeg-devel readline-devel libusbx-devel
 
diff --git a/centos8/run_build.sh b/centos8/run_build.sh
index b6c3401..62aa142 100755
--- a/centos8/run_build.sh
+++ b/centos8/run_build.sh
@@ -10,11 +10,11 @@ echo "Cloned Install-Configurations w/ branch $INSTALL_CONFIG_BRANCH"
 
 if [ "$ADCORE_VERSION" == "NEWEST" ];
 then
-python3 -u installCLI.py -v -c Install-Configurations/$CENTOS8_CONFIG
+python3 -u installCLI.py -v -m -c Install-Configurations/$CENTOS8_CONFIG
 else
 cd Install-Configurations
 echo "Checking out config version $ADCORE_VERSION"
 git checkout -q $ADCORE_VERSION
 cd ..
 fi
-python3 -u installCLI.py -y -s -c Install-Configurations/$CENTOS8_CONFIG -i /epics -p
+python3 -u installCLI.py -y -s -m -c Install-Configurations/$CENTOS8_CONFIG -i /epics -p
diff --git a/config.env b/config.env
index 935e27a..4028e4f 100644
--- a/config.env
+++ b/config.env
@@ -1,7 +1,7 @@
 # Customize URLs to installSynApps tool and Install-Configurations
 INSTALL_SYNAPPS_URL=https://github.com/NSLS-II/installSynApps
-INSTALL_SYNAPPS_BRANCH=master
-INSTALL_CONFIG_URL=https://github.com/jwlodek/Install-Configurations
+INSTALL_SYNAPPS_BRANCH=feature-motor-modules
+INSTALL_CONFIG_URL=https://github.com/NSLS-II/Install-Configurations
 INSTALL_CONFIG_BRANCH=master
 
 # Customize path to configuration for each distribution 
@@ -18,4 +18,4 @@ RHEL8_CONFIG=misc/just-base
 
 # NEWEST if you wish to auto-update to newest releases of 
 # modules, otherwise specify an ADCore version R3-8 or higher
-ADCORE_VERSION=R3-8
+ADCORE_VERSION=NEWEST
